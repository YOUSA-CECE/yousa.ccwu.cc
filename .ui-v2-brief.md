# 任务：为 yousa.ccwu.cc 编写 UI v2 组件层样式

## 你要做的唯一一件事

编辑 `static/ui-v2.css`，在现有内容**之后追加**组件层规则。
不要删改文件里已有的令牌层和基础层（第 1-212 行），只追加。

## 铁律（违反即失败）

1. **绝对不要修改 `static/style.css`** — 一个字节都不能改。它是 v1 回退基线。
2. **绝对不要修改任何 `.html` 模板** — 结构已定，你只写 CSS。
3. 所有规则必须以 `body.ui-v2 ` 前缀开头，用来提升特异性压过 v1。
4. 尽量避免 `!important`。压不过时优先加长选择器（如 `body.ui-v2 .card .card-title`）。
   仅在 v1 用了内联样式或超长选择器时才允许 `!important`。
5. 动效**只能**用 `transform` 和 `opacity`（走 GPU 合成层）。
   禁止动画 `width`/`height`/`top`/`left`/`margin`/`padding`。
6. 不要引入任何外部资源（不要 @import 网络字体、不要外链图片）。纯 CSS。

## 设计令牌（已在文件顶部定义，直接用）

```
--v2-accent: #c45a82        主色（粉）
--v2-accent-2: #4f8faa      辅色（蓝）
--v2-accent-3: #c99f4a      点缀（金）
--v2-text / --v2-text-muted / --v2-text-dim
--v2-panel / --v2-panel-strong / --v2-panel-dark
--v2-border / --v2-border-subtle
--v2-shadow-sm / --v2-shadow / --v2-shadow-lg
--v2-radius: 12px / --v2-radius-sm: 8px / --v2-radius-xs: 6px
--v2-ease: cubic-bezier(0.22,1,0.36,1)
--v2-ease-out: cubic-bezier(0,0,0.2,1)
--v2-duration: 0.25s / --v2-duration-slow: 0.45s
```

## 需要覆盖的组件分组与选择器

先读 `static/style.css` 了解 v1 对这些类的现有规则，再写覆盖。

### A. 导航与页脚
`.navbar .nav-inner .nav-brand .brand-mark .brand-text .nav-links .nav-link
.nav-search .mobile-top-search .nav-user .nav-login .nav-admin
.footer .footer-sep .ui-switch-link`

要求：导航半透明毛玻璃吸顶；`.nav-link` hover 时下划线从中间向两边滑开（用
`::after` + `transform: scaleX()`）；当前页高亮；`.brand-mark` hover 轻微
旋转或缩放。

### B. 首页
`.home-shell .profile-card .profile-cover .profile-avatar .profile-kicker
.profile-name .profile-bio .profile-stats
.hero .hero-eyebrow .hero-title .hero-sub .hero-actions
.feature-grid .feature-card .feature-index
.activity-panel .activity-head .activity-stats .activity-grid .activity-column
.activity-column-title .activity-item .activity-empty
.section .section-kicker .section-title
.card-grid .card .card-title .card-desc .card-link .card-icon
.dashboard-panel .tool-grid .tool-item .tool-name .tool-tag`

要求：`.feature-card` / `.card` / `.activity-item` hover 时
`transform: translateY(-4px)` 加阴影加深，过渡用 `--v2-ease`。
`.feature-index` 做成大号淡色水印数字。
`.hero-title` 字号加大、字重加粗、字距收紧。
`.profile-avatar` 加渐变描边环。

### C. 按钮与表单
`.btn-primary .btn-secondary .btn-danger .form-btn .form-btn-sm .form-input
.role-select .inline-form .search-type`

要求：主按钮渐变背景（accent → accent-2）；hover 抬升 2px；
`:active` 时 `transform: translateY(0) scale(0.98)` 给按压反馈；
`.form-input` focus 时描边变 accent 色并加柔和 focus ring
（`box-shadow: 0 0 0 3px rgba(196,90,130,0.15)`）。
所有可点击元素必须有可见的 `:focus-visible` 样式。

### D. 内容页
`.wiki-layout .wiki-sidebar .sidebar-title .wiki-tree .tree-item .tree-children
.tree-file .wiki-content .wiki-page-title .wiki-search .wiki-empty
.markdown-body .breadcrumb .page-header .page-desc
.blog-list .blog-card .blog-card-title .blog-title-link .blog-excerpt .blog-meta
.search-page .search-hero .search-card .search-list .search-result-item
.search-results .search-snippets .search-summary .search-empty
.about-shell .about-hero .about-grid .about-card .about-actions
.app-download-page .app-download-hero .app-download-card .app-download-title
.app-download-subtitle .app-download-button .app-phone-icon
.app-install-guide .app-release-notes
.file-list .file-item .file-link .file-icon .file-name .file-meta
.empty-state .error-page .error-code .error-message`

要求：`.markdown-body` 排版精修（标题层级、代码块、引用、表格、
行内 code 用 `--v2-font-mono`）。`.tree-item` hover 左侧出现 accent 色指示条。
`.file-item` hover 背景微亮 + 图标轻微放大。

### E. 交互重区（谨慎，这两个页面内联样式多）
`.chat-container .chat-messages .chat-input-area .msg .msg-item .msg-user
.msg-avatar .msg-bubble .typing-dots
.dog-chat .dog-chat-toggle .dog-chat-panel .dog-chat-head .dog-chat-messages
.dog-chat-form .dog-msg .dog-msg-assistant .dog-msg-user .dog-msg-avatar
.pixel-dog .dog-* (像素小狗各部件)
.admin-section .admin-actions .admin-table .admin-table-wrap .admin-user-table
.role-badge .role-admin .role-user .session-info .source-badge .source-app .source-web
.auth-container .auth-card .auth-title .auth-form .auth-hint .auth-back
.guestbook-wrap .flash .flash-success .flash-error .flash-warning .flash-info
.glass-panel .text-muted`

要求：聊天气泡圆角差异化（用户侧/助手侧不同圆角）；
`.dog-chat-toggle` 待机时缓慢呼吸动画（`transform: scale()`），hover 放大；
`.dog-chat-panel` 展开用 `opacity` + `transform: translateY()` 过渡。
**像素小狗的各个 `.dog-*` 部件是精确像素定位的，只调颜色和整体 transform，
不要改它们的 position/width/height，否则小狗会散架。**
`.admin-table` 隔行底色 + hover 整行高亮。

### F. 移动端适配

沿用 v1 已有的两档断点：

```css
@media (max-width: 900px) { body.ui-v2 { ... } }
@media (max-width: 640px) { body.ui-v2 { ... } }
```

要求：
- ≤900px：`.home-shell` 从两列变单列（profile-card 移到上方）；
  `.wiki-layout` 单列；`.feature-grid` 两列
- ≤640px：`.feature-grid` 单列；`.nav-links` 横向滚动或折叠；
  `.activity-grid` 单列；字号适度缩小
- 所有可点击元素触控区 ≥44px（用 `min-height` 而非 padding 撑）
- 保留 v1 的 `env(safe-area-inset-*)` 处理，不要覆盖掉
- `.dog-chat` 浮窗在小屏不能挡住主要内容，面板宽度改为 `calc(100vw - 2rem)`
- 移动端把 `.aurora` 的 `filter: blur()` 半径降到 24px 左右，减轻掉帧

### G. 收尾

在文件最末尾追加：
1. `@media (prefers-reduced-motion: reduce)` 块，把你新加的所有动画/过渡关掉
   （文件里已有一个，你可以再加一个补充你新增的选择器）
2. `@supports not (backdrop-filter: blur(1px))` 降级块，补上你新用到毛玻璃的元素

## 验证

写完后运行，确认无语法错误：

```
python -c "import re,io; s=open('static/ui-v2.css',encoding='utf-8').read(); assert s.count('{')==s.count('}'), f'brace mismatch {s.count(chr(123))} vs {s.count(chr(125))}'; print('CSS_BRACES_OK', len(s))"
```

再确认 style.css 没被碰过：

```
git diff --stat static/style.css
```

这条必须输出为空。如果有输出，立刻 `git checkout -- static/style.css` 还原。

## 交付

完成后简要说明：新增了多少行、覆盖了哪几组、有没有哪个组件因为 v1 特异性
太强而不得不用 `!important`。
